# ============================================================
# mcp-florinda-server — application.properties
# Porta: 8085
# Protocolo: MCP via SSE
# ============================================================

quarkus.application.name=mcp-florinda-server
quarkus.http.port=8085

# ------------------------------------------------------------
# MCP Server — endpoint SSE
# O agente IA conecta em: http://localhost:8085/mcp/sse
# ------------------------------------------------------------
quarkus.mcp.server.sse.root-path=/mcp

# ------------------------------------------------------------
# REST Clients — ms-pedidos e ms-catalogo
# ------------------------------------------------------------
quarkus.rest-client.pedidos-api.url=${PEDIDOS_URL:http://localhost:8080}
quarkus.rest-client.pedidos-api.connect-timeout=3000
quarkus.rest-client.pedidos-api.read-timeout=5000

quarkus.rest-client.catalogo-api.url=${CATALOGO_URL:http://localhost:8082}
quarkus.rest-client.catalogo-api.connect-timeout=3000
quarkus.rest-client.catalogo-api.read-timeout=5000

# ------------------------------------------------------------
# OpenAPI / Swagger
# ------------------------------------------------------------
quarkus.smallrye-openapi.info-title=Florinda Eats — MCP Server
quarkus.smallrye-openapi.info-version=1.0.0
quarkus.swagger-ui.always-include=true

# ------------------------------------------------------------
# OpenTelemetry
# ------------------------------------------------------------
quarkus.otel.exporter.otlp.endpoint=${OTEL_ENDPOINT:http://localhost:4317}
quarkus.otel.resource.attributes=service.name=mcp-florinda-server,service.version=1.0.0

# Health / Metrics
quarkus.smallrye-health.root-path=/q/health
quarkus.micrometer.export.prometheus.path=/q/metrics

# Logs
quarkus.log.level=INFO
quarkus.log.category."br.com.florinda".level=DEBUG

# ============================================================
# PROFILE: dev
# ============================================================
%dev.quarkus.swagger-ui.always-include=true

# ============================================================
# PROFILE: test
# ============================================================
%test.quarkus.log.level=WARN
